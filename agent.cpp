#include "task.h"
//#include "bfs.h"

int main()
{
   Task task_info("input.txt");
   task_info.PrintTaskInfo();
   // run BFS
   if (task_info.task == 1){
       // BFS(task_info);
   }
   return (1); 
}

